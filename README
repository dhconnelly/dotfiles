# dotfiles

## usage

- install:
    - alacritty
    - rustup
    - go
    - fzf from source
    - ripgrep
    - (if necessary) git-prompt: https://raw.githubusercontent.com/git/git/master/contrib/completion/git-prompt.sh

- symlink:
    - .config/alacritty/alacritty-{platform} to ~/.config/alacritty.toml
    - .tmux-{platform}.conf to ~/.tmux.conf
    - .bash{rc,_profile} to ~/.bash{rc,_profile}

